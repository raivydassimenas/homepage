{
  "name": "homepage",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tailwindcss -i ./src/input.css -o ./src/output.css --watch",
    "build:css": "pnpm exec tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "build:html": "cp ./src/index.html ./dist/index.html",
    "build": "mkdir -p dist && pnpm run build:css && pnpm run build:html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "tailwindcss": "^4.2.2"
  }
}
